"use client";

import React from 'react';

const TavusVideo = ({ character }: { character: string }) => {
  // Replace with actual Tavus SDK/API integration
  return (
    <div>
      <h2>Video Interaction with {character}</h2>
      <p>Video content will be displayed here using Tavus.</p>
    </div>
  );
};

export default TavusVideo;
